import { Command } from "./command";
// import { quiz } from "./quiz";
// import { scores } from "./scores";

//TODO Make async and fetch data every time
export const commands = new Map<string, Command>([
  // [quiz.getName(), quiz],
  // [scores.getName(), scores],
]);
